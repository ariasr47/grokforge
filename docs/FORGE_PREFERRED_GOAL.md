# Forge preferred platform — continuous measured improvement

**Surface:** the whole Forge app (Code, Chat, desktop chrome, composer, docks, motion).  
**Engine:** in-house **grok-acp** on both tabs. Do not spawn `grok.exe agent stdio`.  
**Log:** append one observe / elect / change / prove line per slice in §9.  
**Launch:** paste the `/goal` block in §0.

This **supersedes the STOP bar** in `docs/ACP_CODE_GOAL.md` and `docs/CHAT_DAILY_GOAL.md`. Those files stay as evidence of past bars. They are not the destination. Pause is first-class and **only** the operator can stop the loop.

We are not a TUI skin, not a VS Code clone, not a second Grok Build marketplace.

---

## 0. Launch (`/goal`)

```
/goal Forge as the preferred platform (docs/FORGE_PREFERRED_GOAL.md). Whole app. grok-acp only — never grok.exe / grok agent stdio. Operator is the only STOP; a green table is not STOP. Loop: dogfood Desktop Forge Dev (host :8788, banner host :8788) → elect one live FAIL (function/honesty, then clip/shout, then polish) → smallest fix → prove with a test that would have failed before if chrome/behavior can silently regress, plus recapture png+json+txt → log the delta in that file §9 → immediately dogfood again. Close the defect class, not only the symptom (post-terminal allow-list, sibling-run docks, Denied vs Answered). One host. No second :8788. Occupied :8788: attach or kill the owned pid, never both. No KEEP series. No inventing MCP/skills/subagent/browser engines. Identity is Grok. Do not mutate shared fixtures (ACP-A3.md). Follow-up execute must click Review docks (Allow once / Accept). Seed if live has not shown worse: Revert edit; Pause/Cancel; long-turn File changes under sticky You; busy second send. Invent harder dogfood as you go.
```

---

## 1. Why this exists

Named journeys (Chat C*, Code A*/D*/E*/F*) proved the engine flip and a daily-job floor. They also taught the wrong lesson: **green twice + Q clean = done**.

The product you want is the app you open first, all day, instead of Claude Code, Codex, Cursor, Claude.ai, or ChatGPT. That is not a finite table. It is a loop of using Forge, noticing what is worse, and making a **measured** change.

Exceed those tools. Do not copy their layout or their marketplace.

---

## 2. Goal kind

`code-change` with **live dogfood** of the running app. Whole-app. Chat and Code both in play. Shared chrome (composer, docks, Your turn, wait copy, export, hierarchy) is in scope on whichever tab you are using.

---

## 3. What “better” means (measure, don’t vibe)

A slice is only done if **one axis** moved in a way a stranger could check. Pick the axis that the elected defect actually lives on.

| Axis | Better looks like | Prove with |
|------|-------------------|------------|
| **Function** | A daily job completes (send, Review write, Plan accept then edit, test, git via shell, follow-up memory, export, stop/retry/copy) | Disk / tool output / JSON observables match the job |
| **Honesty** | Failures, identity, and “not run” are true; no hydrating lies; no fake catalogs | Shot + JSON; no Mini-Grok / Grok Code consolation; no dump markers |
| **Hierarchy** | You / Thought / Plan / File changes / tools / Answered / Your turn / composer don’t cover each other | Layout flags (`*Below*` true, `*CoveredBy*` false) + screenshot |
| **Voice** | Sentence-case; idle **Your turn**; wait **Waiting for model…**; Thought ≠ answer | Body text; no shouty uppercase instruction |
| **Interaction** | Docks settle; composer enables when it should; Deny/Retry/Revert/Pause/Stop do what they say | Click log + composer enabled + disk |
| **Density** | One turn scannable at 1440×900 without hunting | Screenshot at that size |
| **Motion / perf** | No max-update-depth; no black-crash; no jank on send | Console; `maxDepth: false` |
| **Desktop = web** | Same observables on Forge Dev | Banner `host :8788`; `:8810` down |

Taste without a hidden job (prettier with no clip, no lie, no blocked send) is still in scope **if** you can name the before/after (e.g. wait heading was uppercase, now it isn’t). Unnamed “make it nicer” is not a slice.

**Out (still refuse):** MCP marketplace, vendor skills catalog, computer use, Custom GPTs, voice, pixel-parity with Claude Code’s VS Code skin, re-enabling `grok agent stdio`.

---

## 4. The loop (this is the work)

Repeat until the operator says pause. Do not invent a Wave N table and then STOP when it is checked.

1. **Dogfood** — Drive Forge Dev (prefer Desktop CDP `:9222`, else DEV web `:5174`). Do a real job: a coding edit, a Chat rewrite, a Plan, a test run, an export, a follow-up. Invent the next job if the last one was easy.
2. **Observe** — Watch the live UI the way a user would. Read the shot, JSON, host log, composer enabled, docks, overlap flags, console.
3. **Evaluate** — Rank friction. Daily-blocked function and honesty beat clip. Clip and shout beat idle polish. Shared chrome that is wrong on **both** tabs beats a Code-only nit.
4. **Elect** — Exactly **one** defect. First FAIL in that ranking. Do not parallel-fix five things.
5. **Change** — Smallest fix. Chrome/behavior that can silently regress needs a test that **would have failed before**. Host/agent: prefer grok-acp/server truth over shell lies. If the same class will recur (post-terminal events, global dock queues, Answered-on-deny), close the class in the same slice.
6. **Prove** — Recapture the same job (png+json+txt). Quote the measured delta (e.g. `fileHeadBelowYou: false → true`; dock dismissed; wait copy `Waiting for model…`; test 1 pass). Whole-app: if you touched shared chrome, glance the other tab once.
7. **Log** — One broke/fix/re-check line in §9. Then go to 1. Do not wait for a Quality pass to invent the next job.

**Host rules:** one `:8788`. Occupied port: attach **or** kill the owned pid, never both. Desktop banner must be `host :8788`. Never start a second host. Scratch captures under temp only. Dedicated probe files under `docs/dogfood/acp-code/` — never rewrite `ACP-A3.md`. Follow-up execute (`FORGE_FOLLOWUP`) must click **Allow once** / **Accept**, not just wait for Your turn.

---

## 5. Seed friction (starting backlog — not closed)

Use these if live dogfood has not yet shown something worse. Drop any item the moment a worse live defect appears.

| Seed | Why it is here |
|------|----------------|
| **Invent the next job** | Live dogfood; no remaining named seed this round |

**Done this round (do not re-elect unless live regresses):** Accept plan records; Plan → do write under Review; Deny is honest; Denied (not Answered) + Retry; at-rest Thought below You; shouty wait copy; File changes owns Revert chrome; Review Accept stamps guarded revert (Revert offered; disk restored); one Review write is one Activity row; collapsed Thought stays below sticky You after FILES/TOOLS; concurrent second prompt is `run_active` 409; Chat glance C-bar; uncommitted Chat homes show auto-title from first You; Answered does not keep composer Cancel; Cancel during admission stops the run; expanded You no longer covers File changes / tools; expanding You keeps Answered / Your turn above the composer; pause is one toast (Stopped by you replaces Cancel requested); toasts sit above the composer so Export stays clickable; collapsed Git review is a compact fold; Chat follow-up history includes the prior vouched answer; two-line You does not offer Show more from the 110-char heuristic.

Prior evidence: `docs/ACP_CODE_GOAL.md` §12, `docs/CHAT_DAILY_GOAL.md`, `{SCRATCH}` under `%TEMP%\grok-goal-acp-code\`.

---

## 6. Constraints that stay

- grok-acp only. No vendor CLI for Code or Chat.
- Elect from the first FAIL only.
- Chrome slices: a test that would have failed before the fix.
- No KEEP overwrite series. No second host.
- Do not invent MCP / skills / subagent / browser engines.
- Do not “fix Code” by switching to Chat, or the reverse.
- Identity is first-class **Grok**, not Mini-Grok, not a Grok CLI badge.

---

## 7. Non-goals

- Finishing E5–F7 or D4–D9 as a substitute for this loop (those IDs may be dogfood jobs; they are not the goal).
- Q-clean STOP. Q is a **lens** you can walk after a cluster of greens, never a finish line.
- Rewriting VISION.md wholesale.
- Commit/push unless asked.

---

## 8. Acceptance / stop

There is **no** “current set green twice and Q clean.”  

The loop is succeeding when each logged slice has a measured delta and the next dogfood session is using the app again.

**Stop** only when the operator says pause.

---

## 9. Slice log

- **Open / 2026-08-31:** Operator asked for a whole-app continuous goal: dogfood, observe, evaluate, change; measured improvement in any aspect; preferred platform vs Claude Code / Codex / similar. This file is the operating `/goal`. Seed FAIL from live Code: **Accept plan does not dismiss the dock** (composer stays Settle the card below; no host `/api/plan` settle in the log). Hierarchy clip on long expanded turns remains. Do not STOP.
- **Accept-plan / 2026-08-31:** Broke: shell `reduceRunEvent` dropped `decision_request` / `plan_record` when `eventSeq > terminalEventSeq`, so Accept left `decisions.p1.status` **pending** and the composer on **Settle the card below**. Fix: after terminal, still fold `decision_request` and `plan_record` (post-terminal `answer_delta` stays dropped). Re-check: `{SCRATCH}/unit-preferred.log` `runReducer.test.ts` **45/45** — `decisions.p1.status: pending → accepted`. Desktop `{SCRATCH}/desktop-run1-F0-plan.{png,json,txt}`: click **Accept plan**; `planDock` null; **Plan accepted**; `composerEnabled` true; no Settle the card below; **Your turn**; `maxDepth` false; banner **host :8788**; identity **Grok**; FILES —. Follow-up `{SCRATCH}/desktop-run1-F0-do-done.{png,json,txt}`: Allow once → Accept; File changes `ACP-A3.md` **Accepted**; disk HTML comment; composer **Speak into the continuum**. Chat glance `{SCRATCH}/desktop-chat-glance.{png,json,txt}` after the shared reducer: **CHAT-GLANCE-OK**; **Your turn**; `thoughtBelowYou` true; `maxDepth` false; banner **host :8788**; identity **Grok**. At-rest Code `{SCRATCH}/desktop-code-thought.*`: `thoughtBelowYou` true; **CODE-THOUGHT-OK**. A green table is not STOP.
- **Deny then Retry / 2026-08-31:** Broke (live `{SCRATCH}/desktop-deny.*`): Deny is honest (`User denied write permission`, disk absent) but the Code turn stayed **Answered** with no **Retry** (Chat MessageList / cancelled `RunTerminalNotice` already had it). First fix: Retry after declined write. Follow-up debt: **Denied** (not Answered) for declined permission *or* diff; Retry still offered without `onRetryPrompt` only as copy; `POST_TERMINAL_EVENT_TYPES` named allow-list; `mergePendingDiffs` / `mergePendingPermissions` keep sibling-run queues; `waitIdleAgain` clicks Allow once / Accept; `ACP-A3.md` restored to fixture `ACP-A3-OK`. Tests: `RunTerminalNotice.test.tsx`; `runChangeList` sibling-run merge. A green table is not STOP.
- **Optimized launch / 2026-08-31:** Operator asked for another improvement goal, optimized. §0 one-shotter drops solved seeds, requires closing the defect class, fixture hygiene, and Review-dock clicks on follow-up. Next elect from seed: **Revert edit**, **Pause/Cancel**, or long-turn File changes clip — unless live dogfood shows worse. Do not STOP.
- **Revert chrome class / 2026-08-31:** Broke (live `{SCRATCH}/desktop-revert.*`): Revert restored disk (probe absent) but **Edit reverted** appeared in File changes **and** twice under Activity (`getAllByText` would be 3). Fix: when `changeListCoversActivity`, Activity no longer repeats Revert edit / Edit reverted / Edit not reverted — File changes owns that chrome (same class as View diff). Re-check: `{SCRATCH}/unit-preferred.log` inspectable + recovery tests **52/52** — `Review File changes revert success is not repeated in Activity`. Recapture `{SCRATCH}/desktop-revert-recapture.*`: banner **host :8788**; identity **Grok**; `maxDepth` false; `fileHeadBelowYou` true; File changes **Accepted** (this run did not expose Revert — recoveryAvailable missing). Probe file left on disk from recapture; fixture `ACP-A3.md` untouched. Chat glance skipped (File changes is Code-only). A green table is not STOP. Next elect: Revert not offered after some Review Accepts; Pause/Cancel; long-turn clip.
- **Review Accept stamps Revert / 2026-08-31:** Broke (live `{SCRATCH}/desktop-revert-recapture.*`): Accept left File changes **Accepted** with no **Revert edit** because host `retainActivityAfterDiff` always set `recovery: null`, wiping grok-acp’s guarded-revert stamp after the edit applied. Fix: accept stamps `{ kind: "guarded_revert", available: true, status: "available" }`; reject stays `null`. Re-check: `{SCRATCH}/unit-preferred.log` host `activity-path` + `activity-kind` **13/13** — `retainActivityAfterDiff accept stamps guarded revert so Review offers Revert edit`. Recapture `{SCRATCH}/desktop-revert-available.*` after host restart (pid 14212 → 52528): clicks Allow once → Accept → **Revert edit**; File changes **Reverted** + **Edit reverted** once; Activity write-file **Completed** only (no second Edit reverted); probe **absent**; fixture `ACP-A3.md` `ACP-A3-OK`; `fileHeadBelowYou` true; `thoughtBelowYou` true; `maxDepth` false; banner **host :8788**; identity **Grok**; composer enabled; **Your turn**. Chat glance skipped (host recovery stamp, Code-only). Leftover: Tool activity lists the same write twice. A green table is not STOP. Next elect: long-turn File changes under sticky You; busy second send; Pause/Cancel already observed PASS.
- **One Review write, one Activity row / 2026-08-31:** Broke (live `{SCRATCH}/desktop-revert-available.*` + `{SCRATCH}/desktop-hierarchy.*`): File changes had **1** path but Tool activity listed the write **twice** (`TOOLS 2`). Host `activityRecordFromProposedEdit` keyed `activityId` to `editId` while grok-acp `tool_run` keys `activityId` to `call.id`, so the reducer kept two records. Fix: proposed edit reuses `invocationId` as `activityId` so pending tool_run, proposed, terminal, and retain fold onto one row. Re-check: `{SCRATCH}/unit-preferred.log` host **14/14** — `proposed edit reuses the tool-run activityId so Review Activity is one write` (RED `edit-dup` vs `call-dup`). Recapture `{SCRATCH}/desktop-one-write.*` after host restart (pid 52528 → 45912): **TOOLS 1**; one write-file **Completed**; File changes `one-write.md` **Accepted** + **Revert edit**; disk `ONE-WRITE-OK`; fixture `ACP-A3.md` `ACP-A3-OK`; `fileHeadBelowYou` true; `maxDepth` false; banner **host :8788**; identity **Grok**. Chat glance skipped (host activity identity, Code-only). Leftover: `thoughtCoveredByYou` true after FILES/TOOLS jump. A green table is not STOP. Next elect: Thought tucked under sticky You; busy second send.
- **Thought stays below You / 2026-08-31:** Broke (live `{SCRATCH}/desktop-one-write.*`): after FILES then TOOLS, `thoughtCoveredByYou` true — collapsed Thought sat at y=191 under sticky You (132–213). Fix: FILES jump treats Thought under You as a miss and scrolls it clear; `--run-file-stick-below` includes collapsed Thought height; collapsed Thought is sticky at `--run-prompt-stick-below` (z-index 4, under You). Re-check: `{SCRATCH}/unit-preferred.log` OverviewStrip **8/8** — `FILES jump detects collapsed Thought tucked under sticky You`; quiet-chrome collapsed Thought **sticky**. Recapture `{SCRATCH}/desktop-thought-sticky.*`: `thoughtBelowYou` **false → true**; `thoughtCoveredByYou` **true → false**; Thought chip visible between You and File changes; **TOOLS 1**; `fileHeadBelowYou` true; `maxDepth` false; banner **host :8788**; identity **Grok**; fixture `ACP-A3.md` `ACP-A3-OK`. Chat glance skipped (Code run Thought). A green table is not STOP. Next elect: busy second send; Pause/Cancel already observed PASS.
- **Busy second send / 2026-08-31:** Broke (class): host `prompt()` checked `busy` then awaited `acquireCodeAgent` before `setBusy(true)`, so two POSTs on the same session could both admit. Shell `sendText` only refused `ownedRunActive`, not session-busy / in-flight. Fix: latch `setBusy(true)` before any await and throw `run_active`; shell `composerSendAdmitted` refuses session-busy and a page in-flight latch. Re-check: host `concurrent second prompt on the same session is run_active` **202+409**; shell `composerSend.test.ts` **6/6**. Live `{SCRATCH}/desktop-busy-second-final.*`: `maxDepth` false; banner **host :8788**; identity **Grok**. Host log shows sequential prompts (first `agent done`, then a new ACP session) rather than overlap — TURNS 2 is a follow-up after the first run ended. Fixture `ACP-A3.md` `ACP-A3-OK`. Leftover: composer Cancel can still be up after Answered. A green table is not STOP. Next elect: Chat glance after Code chrome; busy chrome lag.
- **Chat auto-title / 2026-08-31:** Broke (live `{SCRATCH}/desktop-chat-glance.*`): Chat glance held C-bar (`CHAT-GLANCE-OK`, **Your turn**, Export, `thoughtBelowYou` true, `maxDepth` false, banner **host :8788**, identity **Grok**, Send not Cancel) but three turned chats all listed **Name this home**. Auto-title from first You was stored (`saveSessionMessages`) then hidden whenever `committedName` was false. Fix: `chatListTitle` shows the auto-title until the user commits a name; placeholder only on empty/default **New chat**. Re-check: `{SCRATCH}/unit-preferred.log` `chatListTitle shows auto-title so uncommitted homes are distinguishable`; ChatHomeName **6/6**. Recapture `{SCRATCH}/desktop-chat-glance-title.*`: heading + list **Reply with exactly CHAT-GLANCE-OK…**; empty 12h-ago row still **Name this home**; fixture `ACP-A3.md` `ACP-A3-OK`. A green table is not STOP. Next elect: busy chrome lag (Cancel after Answered).
- **Answered does not keep Cancel / 2026-08-31:** Broke (class): composer `busy` was `activeRun || runStartedAt`, so after the owned run was terminal (`Answered`) a leftover `runStartedAt` kept **Cancel** until an effect cleared it. Fix: `composerChromeBusy` is false when `ownedAllTerminal` even if `runStartedAt` is still set; Cancel only while a non-terminal run or pre-admission wait. Re-check: `{SCRATCH}/unit-preferred.log` composerSend **7/7** — `Answered owned run does not keep Cancel just because runStartedAt is still set`. Recapture `{SCRATCH}/desktop-idle-send.*`: **Answered** + **Your turn**; `composerSend` true; `composerCancel` false; `IDLE-SEND-OK`; `maxDepth` false; banner **host :8788**; identity **Grok**; fixture `ACP-A3.md` `ACP-A3-OK`. A green table is not STOP. Next elect: Pause/Cancel (already observed PASS unless live regresses); long-turn hierarchy.
- **Cancel during admission / 2026-08-31:** Broke (live `{SCRATCH}/desktop-pause-recheck.*`): Stop was clicked (`cancel requested`) **before** the host prompt was admitted, then the agent started anyway and the turn **Answered** a 20-line list — no **Stopped by you**, no **Retry**. Fix: `cancelDuringAdmission` aborts the POST if Cancel beat admission, or cancels the admitted run; do not clear that intent when `sendText` awaits `/api/prompt`. Re-check: composerSend **8/8**. Recapture `{SCRATCH}/desktop-pause-recapture.*`: clicks send → Stop; **Cancelled**; **Retry**; **Stopped by you**; composer **Send**; `composerCancel` false; not Answered; **Your turn**; `maxDepth` false; banner **host :8788**; identity **Grok**; fixture `ACP-A3.md` `ACP-A3-OK`. A green table is not STOP. Next elect: long-turn hierarchy.
- **Expanded You no longer covers File changes / 2026-08-31:** Broke (live `{SCRATCH}/desktop-long-hierarchy-expand.*`): Show more (`.run-prompt-more` span, not a button role) expanded You to **h=427** and File changes sat at y=229 under it (`fileHeadBelowYou` false, `toolsHeadBelowYou` false). Fix: cap `.run-prompt.is-expanded` at `min(36vh, 280px)` with a scrolling body, and `scrollDeltaToClearStickyYou` nudges the transcript when You grows over File changes / tools / Thought. Re-check: `{SCRATCH}/unit-preferred.log` OverviewStrip + quiet-chrome + RunSurface **49/49** — `You height growth that tucks File changes under sticky You needs a transcript nudge` (RED `0 !== -338`); expanded You overflow auto. Recapture `{SCRATCH}/desktop-long-hierarchy-recapture.*`: You **427→245**; `youExpanded` true; **Show less**; File changes y **229→384**; `fileHeadBelowYou` **false→true**; `toolsHeadBelowYou` **false→true**; Thought below You; **TOOLS 1**; Revert edit; disk `HIERARCHY-OK`; fixture `ACP-A3.md` `ACP-A3-OK`; `maxDepth` false; banner **host :8788**; identity **Grok**; composer **Send**. Dogfood clicks `.run-prompt-more` after FILES/TOOLS (role=button Show more was a no-op). Leftover: `answeredCoveredByComposer` / `yourTurnCoveredByComposer` true after expand. A green table is not STOP. Next elect: Your turn under composer after expanded You; stacked pause toasts unless live shows worse.
- **Your turn stays above the composer after expand / 2026-08-31:** Broke (live `{SCRATCH}/desktop-long-hierarchy-recapture.*`): expanding You to read File changes left **Answered** at y=821 and **Your turn** at y=857 under the composer (`answeredCoveredByComposer` / `yourTurnCoveredByComposer` true). Fix: after the files nudge, `youHeightToKeepCueVisible` shrinks expanded You so the cue can fit, then `scrollDeltaToKeepCueAboveComposer` scrolls the leftover gap without tucking File changes under You. Re-check: `{SCRATCH}/unit-preferred.log` OverviewStrip + quiet-chrome + RunSurface **50/50** — `expanded You shrinks so Your turn stays above the composer` (RED `245 !== 105` then `0 !== 18`). Recapture `{SCRATCH}/desktop-cue-above-composer.*`: `yourTurnCoveredByComposer` **true→false**; `answeredCoveredByComposer` **true→false**; Your turn y **857→718**; Answered y **821→682**; File changes still below You; **Show less**; `fileHeadBelowYou` true; `toolsHeadBelowYou` true; `thoughtBelowYou` true; **TOOLS 1**; disk `HIERARCHY-OK`; fixture `ACP-A3.md` `ACP-A3-OK`; `maxDepth` false; banner **host :8788**; identity **Grok**; composer **Send**. Chat glance `{SCRATCH}/desktop-chat-glance-after-cue.*`: **CHAT-GLANCE-OK**; **Your turn**; `yourTurnCoveredByComposer` false. Leftover: stacked pause toasts. A green table is not STOP. Next elect: stacked pause toasts unless live shows worse.
- **Pause is one toast / 2026-08-31:** Broke (live `{SCRATCH}/desktop-pause-toasts.*`): Stop toasts **Cancel requested** then **Stopped by you** (`toastCount` 2) covering **Export** (`exportCoveredByToast` true). Transcript already had Cancelled + Retry + Stopped by you. Fix: `nextToastsAfterPush` replaces Cancel requested when Stopped by you arrives. Re-check: `{SCRATCH}/unit-preferred.log` Toast **2/2** — `Stopped by you replaces Cancel requested so pause is one toast`. Recapture `{SCRATCH}/desktop-pause-toasts-recapture.*`: `toastCount` **2→1**; toasts **Stopped by you** only; `exportCoveredByToast` **true→false**; Export visible; Cancelled + Retry; composer **Send**; `maxDepth` false; banner **host :8788**; identity **Grok**; fixture `ACP-A3.md` `ACP-A3-OK`. A green table is not STOP. Next elect: invent the next job from live dogfood.
- **Toasts sit above the composer / 2026-08-31:** Broke (live `{SCRATCH}/desktop-chat-export.*`): Export wrote `CHAT-EXPORT-OK` markdown but the **Downloaded chat as Markdown** toast sat on **Export** (`exportCoveredByToast` true; stack y=831 vs Export y=812). Fix: `.toast-stack` `bottom: calc(var(--composer-height, 160px) + 12px)` so toasts clear composer chrome. Re-check: `{SCRATCH}/unit-preferred.log` quiet-chrome — `toasts sit above the composer so Export stays clickable` (RED `bottom: 16px`). Recapture `{SCRATCH}/desktop-chat-export-recapture.*`: `exportCoveredByToast` **true→false**; toast y **831→675**; Export clickable; file still `CHAT-EXPORT-OK`; `maxDepth` false; banner **host :8788**; identity **Grok**. Code glance `{SCRATCH}/desktop-code-toast-above-composer.*`: Stopped by you y=675; `exportCoveredByToast` false. A green table is not STOP. Next elect: invent the next job from live dogfood.
- **Collapsed Git review is a compact fold / 2026-08-31:** Broke (live `{SCRATCH}/desktop-git-status.*`): `git status --short` completed (answer `M apps/host/src/activity-path.test.ts`; **TOOLS 1**) but collapsed **Git review 1 / 1 status** was a full-width empty well (~820px). Fix: `.git-review:not(:has(.git-review-list))` is `width: max-content`, `padding: 4px 12px`, `justify-self: start` (same class as collapsed Thought). Re-check: `{SCRATCH}/unit-preferred.log` quiet-chrome + GitReviewSection **36/36** — `collapsed Git review is a compact fold, not a padded empty card` (RED missing rule). Recapture `{SCRATCH}/desktop-git-status-recapture.*`: Git review **w=189 h=36** (You 820); TOOLS y **327→311**; still **TOOLS 1**; answer first status line; `maxDepth` false; banner **host :8788**; identity **Grok**; fixture `ACP-A3.md` `ACP-A3-OK`. Chat glance `{SCRATCH}/desktop-chat-glance-after-git.*`: **CHAT-GLANCE-OK**; no Git review. A green table is not STOP. Next elect: invent the next job from live dogfood.
- **Chat follow-up includes the prior answer / 2026-08-31:** Broke (live `{SCRATCH}/desktop-chat-followup.*`): second turn replied **FOLLOW-MEM-SET** not **FOLLOW-MEM-OK**. Host `historyTurns: 1` — run-backed answers skip `text_delta` into `messages`, so follow-up history was the first You only. Fix: `foldRunAnswersIntoHistory` inserts the vouched run answer after a user with `projectedRunId` when messages have no assistant. Re-check: `{SCRATCH}/unit-preferred.log` promptSendHistory **2/2**. Recapture `{SCRATCH}/desktop-chat-followup-recapture.*`: second answer **FOLLOW-MEM-SET → FOLLOW-MEM-OK**; host `historyTurns` **1→2**; **TURNS 2**; `maxDepth` false; banner **host :8788**; identity **Grok**. A green table is not STOP. Next elect: invent the next job from live dogfood.
- **Two-line You does not offer Show more / 2026-08-31:** Broke (live `{SCRATCH}/desktop-atfile.*`): `@docs/dogfood/ACP-A3.md` quoted **ACP-A3-OK**, dump hidden, but **Show more** appeared on a 2-line You (`promptBodyMeasuresOverflow` `length > 110`). Fix: length floor **110→180** so wrap-to-two-lines does not force overflow; scrollHeight and 3+ newlines still do. Re-check: `{SCRATCH}/unit-preferred.log` RunSurface artifacts **13/13** (RED `true !== false` on the live @file text). Recapture `{SCRATCH}/desktop-atfile-recapture.*`: `showMore` **Show more → null**; mention `@docs/dogfood/ACP-A3.md`; answer **ACP-A3-OK**; fixture `ACP-A3.md` `ACP-A3-OK`; `maxDepth` false; banner **host :8788**; identity **Grok**. A green table is not STOP. Next elect: invent the next job from live dogfood.
- **File changes header stays below Thought / 2026-09-01:** Broke (live `{SCRATCH}/desktop-d7-parse.*` after View diff): parser fixture landed (parse.js + parse.test.js, `node --test` 1 pass) but File changes header sat at y=242 under collapsed Thought 225–251 (`fileHeadCoveredByThought` true; `fileHeadBelowThought` false). FILES jump only treated Thought-under-You as a miss, so a Thought already below You still covered the header. Stick-below used `:not([open])`, which can miss a closed details. Fix: jump floor is `max(youBottom, thoughtBottom)`; `collapsedThoughtHeightPx` uses details `.open`. Re-check: OverviewStrip + RunSurface artifacts **24/24** — live D7 numbers RED `false !== true`. Recapture `{SCRATCH}/desktop-d7-parse-recapture.*`: header y **242→263**; `fileHeadBelowThought` **false→true**; `fileHeadCoveredByThought` **true→false**; File changes **1** + **Revert edit**; disk `// D7-RECAPTURE`; fixture `ACP-A3.md` `ACP-A3-OK`; `maxDepth` false; banner **host :8788**; identity **Grok**. Leftover: compact Thought is 82px wide, so an open diff body can still show *beside* the chip. A green table is not STOP. Next elect: invent the next job from live dogfood (diff body beside Thought unless worse).
- **Thought covers File changes beside the chip / 2026-09-01:** Broke (live `{SCRATCH}/desktop-d7-parse-recapture.*`): collapsed Thought was `width: max-content` (w=82) so an open diff painted to the right of the chip. A first try made File changes its own scrollport (`header top: 0`) and recapture `{SCRATCH}/desktop-d7-diff-beside-thought.*` was worse — header y **263→89** under You. Reverted that. Fix: collapsed Thought is full-width sticky paint (`justify-self: stretch`, opaque `--bg`, solid 14px shadow) with a compact summary; View diff nudges the card (`transcriptScrollAfterOpenFileDiff`) instead of scrolling the transcript to the end. Re-check: OverviewStrip + quiet-chrome **42/42**. Recapture `{SCRATCH}/desktop-d7-thought-paint-2.*`: Thought w **82→820**; `fileHeadBelowThought` true; File changes **1** visible; no diff lines beside Thought; disk `// D7-RECAPTURE-4`; fixture `ACP-A3.md` `ACP-A3-OK`; `maxDepth` false; banner **host :8788**; identity **Grok**. Chat glance skipped (Code Thought). A green table is not STOP. Next elect: invent the next job from live dogfood.
- **TOOLS keep-end does not hide File changes / 2026-09-01:** Broke (live `{SCRATCH}/desktop-f6-nested.*`): nested `mod.js` + `mod.test.js` and `node --test` 1 pass, but FILES then TOOLS `scrollTop = scrollHeight` yanked `.file-changes` to y=−99 so the sticky header clamped at 242 under Thought (`fileHeadCoveredByThought` true; File changes missing on screen). Fix: `shouldKeepEndAfterToolsJump` is false when the projected File changes header would sit under You/Thought. Re-check: OverviewStrip **13/13** — live F6 numbers RED `true !== false`. Recapture `{SCRATCH}/desktop-f6-nested-recapture.*` after FILES+TOOLS+View diff: header y **242→263**; `fileHeadCoveredByThought` **true→false**; File changes **1** + Revert + tools all visible; disk `// F6-RECAPTURE`; fixture `ACP-A3.md` `ACP-A3-OK`; `maxDepth` false; banner **host :8788**; identity **Grok**. A green table is not STOP. Next elect: invent the next job from live dogfood.
- **Accepted Plan steps stay on their own lines / 2026-09-01:** Broke (live `{SCRATCH}/desktop-plan-do-plan.*`): Plan→do function later passed (Allow once; disk `PLAN-DO-OK`; `ACP-A3.md` untouched) but the accepted Plan card mashed 1./2./3. into h=35 because `.plan-body` is a `<p>` without `pre-wrap`. Fix: dedicated `.plan-body { white-space: pre-wrap; overflow-wrap: anywhere; }`. Re-check: quiet-chrome + PlanSection **42/42** — RED missing dedicated `.plan-body` rule. Recapture `{SCRATCH}/desktop-plan-lines.*`: plan-body h **35→87**; steps 1. 2. 3. each on their own line; FILES —; banner **host :8788**; identity **Grok**; fixture `ACP-A3.md` `ACP-A3-OK`. Leftover: Plan card can sit a few pixels under Thought. A green table is not STOP. Next elect: invent the next job from live dogfood.
