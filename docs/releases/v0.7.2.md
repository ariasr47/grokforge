# Forge 0.7.2 — Grok connectors honesty and Chat mermaid

Follow-up to 0.7.1. Same unsigned Windows NSIS. Settings no longer lists a Forge paste catalog (Gmail / Notion Test / Try in Chat). Chat fenced mermaid draws or stays honest plain.

## Highlights

- **Grok connectors.** Settings shows **Linked status: unknown** and **Open grok.com/connectors**. Forge cannot see which accounts are linked. Forge Chat does not use those links yet. Paste-catalog token/test is parked (`410`).
- **Chat mermaid (C2).** A ` ```mermaid ` fence renders a diagram, or stays plain source if empty or if draw fails. Not a second highlighter.
- **S3.** House Code is Grok (grok-acp). First Code visit does not ask to install Grok CLI. Mini-Grok copy remains only for a real vendor-CLI fallback.
- **Unchanged.** SHA pin in Settings, Review default, unknown-publisher warning, house grok-acp Code.

## Verification

- Host `connectors.honesty.test.ts` **3/3**
- Shell `ConnectorsPanel.test.tsx` + `mermaid.diagram.test.tsx` **7/7**
- Live Chat recapture 2026-09-14: `data-mermaid=drawn` (Forge Chat → Mermaid)
- `npm run build:prod` SHA/size filled below.

## Installer

- Asset: `apps/shell/src-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/Forge_0.7.2_x64-setup.exe`
- SHA-256: `9c39fa3990c77c65ecbd905f8501fa685d206662f23f373aae6d4fbd7d562e1b`
- Size: 29,343,552 bytes
- Updater signature: `Forge_0.7.2_x64-setup.exe.sig` (local key present)
- Windows x64, current-user NSIS installer.
- The installer is not Authenticode-signed, so Windows may show an unknown-publisher warning.

## Upgrade

Close Forge before running the installer. Existing per-user conversations and settings are retained by the current-user upgrade path.
A 0.7.1 machine can pick up 0.7.2 from GitHub via Check for updates (`latest.json`).
