# Forge 0.6.7 — House Grok Code

Forge 0.6.7 is the dogfood cut after 0.6.6: Code runs in-house grok-acp (not `grok agent stdio`), Review/Trusted stay Forge Gates, and Changes / Home / Keep planning are in the published box.

## Highlights

- **House Code engine.** Code turns use Grok via grok-acp. A `grok` CLI on PATH does not divert Code to vendor stdio. Identity chrome is **Grok**, not Mini-Grok consolation, not a Grok Code CLI badge.
- **Gate.** Shell, write, plan (Accept / Keep planning), and ask cards. Review still asks; Trusted may auto-apply eligible confined edits.
- **Changes.** Session-grant writes show; one row per path; git-scale ± for a one-line `write_file` (not a whole-file rewrite).
- **Home.** Empty Chat still lands on Home. A bound Code workspace with an empty session is the Code canvas so AGENTS.md loads.
- **Unchanged.** Chat stays grok-acp. Review is the default. Trusted is saved per workspace. Bypass never persists as an active session mode. The installer is still unsigned.

## Verification

- Host Code-identity tests 14/14 (house acquire, eager stamp, live end does not fall back to Mini-Grok).
- Shell Code-identity tests 22/22 (house chip is Grok; Sign-in gates house without a host key).
- Vendor-spawn host tests (Skills / children / browser / MCP / hooks / `grok agent stdio` journeys) are PARKED under `apps/host/parked-tests/` — house Code does not spawn the vendor CLI. Restore if that engine returns.
- grok-acp write-diff 3/3; shell copy/diff/dock + TranscriptBody + live-turn focused suites green on `cc20da6` plus this preflight.
- Host `npm test` after park: **435 pass / 1 fail** (`EBUSY` rmdir in `chat-pack.integration` teardown). Same file isolation **5/5**.
- `npm run build:prod` 2026-09-08: staged bundle AC-S4 OK; NSIS `Forge_0.6.7_x64-setup.exe`.

## Installer

- Asset: `Forge_0.6.7_x64-setup.exe`
- SHA-256: `22E9A7694383611422F6116607F3C63F99E5E5260FB3235AC3CD3AA1090BE4D8`
- Size: 27,950,633 bytes
- Windows x64, current-user NSIS installer.
- The installer is not Authenticode-signed, so Windows may show an unknown-publisher warning.

## Upgrade

Close Forge before running the installer. Existing per-user conversations and settings are retained by the current-user upgrade path.
Review remains the default. Trusted is saved per workspace. Bypass never persists as an active session mode.
A 0.6.6 install can pick up 0.6.7 from GitHub when Check for updates is wired to this release’s `latest.json` (S4 proof after the tag).
