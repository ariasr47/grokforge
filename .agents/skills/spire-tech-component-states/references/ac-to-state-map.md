# AC to state map

For each SPEC §3 (or R1 product) criterion that is user-visible:

1. Name the **state(s)** where it is observable.
2. Note if the criterion spans transitions (e.g. loading → error).
3. Flag criteria with no state home for rewrite or state addition.

| AC | Primary state | Notes |
| --- | --- | --- |
| (example) empty list explains next step | empty | review or test per risk skill |

Never invent a second matrix file; keep the map inside the role artifact.
