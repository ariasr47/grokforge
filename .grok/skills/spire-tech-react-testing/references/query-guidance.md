# Query guidance

Priority order (when available):

1. Role / accessible name
2. Label text
3. Placeholder / text content
4. Test id only as last resort

Assert outcomes users care about (text, role state, disabled, announced error) before DOM structure.
