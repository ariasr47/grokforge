# Missing field

When UI needs a value INTERFACE does not promise:

1. Name the field and the AC/state that needs it.
2. Refuse to invent the field in client code.
3. Bounce via GATE Z for interface/spec amendment.
4. Continue only work that does not depend on the missing field.
