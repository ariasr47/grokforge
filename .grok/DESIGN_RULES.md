# DESIGN_RULES — compatibility notice

This installed path remains temporarily so guidance and live contracts created by older kit versions still resolve. It is not a design authority and contains no aesthetic, branding, token, component or copy rules.

For current work, apply design guidance in this order:

1. the consumer's project design canon when set — default
   `.spire/clusters/tech/context/DESIGN_SYSTEM.md` with `Status: SET`;
2. the approved feature intent and design direction in its existing contract (bounded feature direction
   when design authority is `NONE DECLARED`);
3. the provider-neutral review method in `skills/spire-tech-design-review/SKILL.md`.

The UX role owns purpose, hierarchy, states and feature direction. The frontend role owns implementation plus rendered visual inspection and correction. QA independently verifies the observable accessibility, interaction, state and evidence claims.

Do not add new pointers to this file. Physical retirement waits for a separate fleet check covering live contract references and locally modified installed copies.
